apiVersion: v1
kind: Service
metadata:
    name: auth
    labels:
        app.kubernetes.io/name: kubernetes-microservices
        app.kubernetes.io/component: auth
spec:
    type: ClusterIP
    selector: 
        app.kubernetes.io/name: kubernetes-microservices
        app.kubernetes.io/component: auth
    ports:
        -   name: auth-port-3001
            protocol: TCP
            port: 3001
            targetPort: auth-port-3001
