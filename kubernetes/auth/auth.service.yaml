apiVersion: v1
kind: Service
metadata:
    name: auth
    labels:
        app.kubernetes.io/name: kubernetes-microservices
        app.kubernetes.io/component: auth
spec:
    type: ClusterIP
    selector: 
        app.kubernetes.io/name: kubernetes-microservices
        app.kubernetes.io/component: auth
    ports:
        -   name: port-auth
            protocol: TCP
            port: 80
            targetPort: port-auth
